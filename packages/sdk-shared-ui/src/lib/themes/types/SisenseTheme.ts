import { sfColors } from '../colors';

export type SisenseTheme = {
  gray: string;
  brand: string;
  faded: string;
  border: string;
  grayText: string;
  shadow: string;
  shadowHover: string;
  sf: typeof sfColors;
};
