import { CompleteThemeSettingsInternal } from '@/types';

/**
 * Themable interface for styled components that require theme settings.
 */
export type Themable = { theme: CompleteThemeSettingsInternal };
