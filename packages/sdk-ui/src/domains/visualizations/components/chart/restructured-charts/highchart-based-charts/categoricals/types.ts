/**
 * Categorical chart types supported by the restructured architecture.
 * These charts don't have traditional X/Y axes and instead use categorical data.
 */
export type CategoricalChartTypes = 'pie' | 'funnel' | 'treemap' | 'sunburst';
